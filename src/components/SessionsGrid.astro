---
// Each card carries a bar showing how far into the twelve weeks it sits.
// On an index of twelve near-identical cards it is the one element that
// differs down the page, and it is the course's own vocabulary doing the
// job a progress indicator is actually good at.
import { getPublishedCollection } from "astro-course-university/content";
import Card from "astro-theme-university/components/Card.astro";
import CardGrid from "astro-theme-university/components/CardGrid.astro";
import { formatCourseDate } from "../lib/dates";

const sessions = await getPublishedCollection("sessions");
const visible = sessions.sort((a, b) => a.data.week - b.data.week);
---

<CardGrid columns={2}>
  {
    visible.map((session) => (
      <Card headingLevel="h2" title={session.data.title} href={`/sessions/${session.id}/`}>
        {session.data.description && <p>{session.data.description}</p>}
        <div
          class="bar week-bar"
          role="img"
          aria-label={`Week ${session.data.week} of 12`}
          style={`--bar-value: ${Math.round((session.data.week / 12) * 100)}%`}
        />
        <p>
          <small>Week {session.data.week} · {formatCourseDate(session.data.date)}</small>
        </p>
      </Card>
    ))
  }
</CardGrid>
